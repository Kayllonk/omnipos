import { Bell, Search, SunMedium } from 'lucide-react'

export default function Topbar({ user }) {
  return (
    <header className="glass rounded-3xl p-4 flex items-center justify-between gap-4">
      <div>
        <div className="text-sm text-slate-400">Bem-vindo de volta</div>
        <h2 className="text-2xl font-black">
          {user?.full_name || user?.username || 'Usuário'}
        </h2>
      </div>

      <div className="flex items-center gap-3 flex-1 max-w-2xl">
        <div className="relative flex-1">
          <Search className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" />
          <input
            placeholder="Buscar venda, produto, cliente, pedido..."
            className="w-full bg-panel2 border border-white/10 rounded-2xl pl-12 pr-4 py-3 outline-none focus:border-primary"
          />
        </div>
        <button className="p-3 rounded-2xl bg-white/5 hover:bg-white/10">
          <SunMedium className="w-5 h-5" />
        </button>
        <button className="p-3 rounded-2xl bg-white/5 hover:bg-white/10">
          <Bell className="w-5 h-5" />
        </button>
      </div>
    </header>
  )
}